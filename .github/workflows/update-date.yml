name: "Update Date"

on:
  workflow_dispatch:
  push:
    branches: [ SEMESTER_6, main ]

jobs:
  update-date:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Update Date
        run: date +"%D" > README.md
