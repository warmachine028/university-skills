
int evaluate(char expression[])
{
    int result;
    // LOGIC
    return result;
}

void main()
{
    char expression[100];
    printf("Enter an expression: ");
    gets(expression);
    int result = evaluate(expression);
    printf("Evaluation of %s is %i", expression, result);
}